# Host: e5ae148be928

Per-host overrides for this machine. Not symlinked automatically — append or
`@`-include from the global CLAUDE.md only if you want it active here.

## Candidates flagged during migration (2026-09-02)

The `find` trap currently living in the shared `CLAUDE.md` is host-specific:

> `find` here is a shell function wrapping `bfs` 4.1.1, which rejects the words
> `yesterday`/`today` outright.

That is true on this container because of its interactive shell function. Other
fleet hosts likely have plain GNU `find`, where only the second half of the note
(GNU `find` parses `today` as *now*, not midnight) applies. Decide whether to:

- leave it in the shared `CLAUDE.md` (harmless, mildly confusing elsewhere), or
- move the bfs sentence here and keep only the GNU caveat shared.

No decision has been made; nothing was moved.
