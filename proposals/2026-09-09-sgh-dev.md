# Config proposals — 2026-09-09 — sgh-dev

- Generated: 2026-09-09T06:56:17Z
- Transcripts reviewed: 11 (modified since 2026-09-08T09:50:33)
- Model: opus

Automated output. Read it, edit it, then merge to `main` by hand.

---

Proposals written to `/home/bara/.claude/plans/you-are-reviewing-claude-warm-sky.md` — paste-ready blocks are in that file, in this order:

### 1. Predict-core multi-branch integration workflow
- **Target**: `skills/predict-core-integration/SKILL.md` (new)
- **Evidence**: 4 sessions — `2d465b89`, `44796f47`, `2b36d9de`, `96cb781b`. Three traps re-derived from scratch each time: containment goes stale when a teammate pushes after your merge; reverting the *first* of stacked merges takes later branches' work with it (verify against an independent rebuild, byte-identical trees); tagging `ml-dev-sgh.vNN` triggers a real deploy and consumes the name.

### 2. sgh-dev is an unprivileged container — no cron, no systemd, nested `claude -p` fails
- **Target**: `hosts/sgh-dev.md` — **host-specific**, must not go in shared `CLAUDE.md` (`hosts/sgh-server.md` documents system cron; this box has none)
- **Evidence**: `d463cf4c`, `ce24fded`. Includes the user's own framing: a nightly run that "finds nothing" is indistinguishable from one that failed.

### 3. Do not test `fleet-sync.sh` against the live repo
- **Target**: `runbooks/fleet-config-sync.md` — Do NOT + one symptom row
- **Evidence**: `d463cf4c`. `NO_PUSH=1` suppresses the push, not the commit or checkout; swept five unrelated WIP files onto a sync branch (recovered as PR #3).

### 4. ClaimSense has no runbook
- **Target**: `runbooks/claimsense.md` (new)
- **Evidence**: `8ce9246f`. Carries the two corrections the user had to give — prod config comes from the Argo overlay, not the checked-in `.env`, so an empty `SERVING_DB_URL` locally silently no-ops all DB writes — plus the `isRule`-gates/`isAi`-irrelevant eval semantics that took three rounds.

### 5. Profile corrections
- **Target**: `BaraAlSedih.md`
- **Evidence**: `8ce9246f` (scopes the "No Kubernetes" claim — true of the shared box, false for the Argo/kustomize-deployed analytics app), plus a confirmed working preference (handoff prompts, asked for 3× in one session) and three dated current-project entries.

### 6. Why `mcp-profiler` is deliberately not in the integration branch
- **Target**: `decisions/mcp-profiler-separate-deploy.md` (new)
- **Evidence**: `2d465b89` + `2b36d9de` re-derived the intent from revert commit `20d7b0c`. Rules out restoring the "missing" profiler files, and flags that the revert also dropped the schema-awareness fix for `_add_missing_columns` — without it the profiler `ALTER`s `public` for every tenant.

### 7. Handoff-prompt slash command
- **Target**: `commands/handoff-prompt.md` (new; `commands/` is currently empty)
- **Evidence**: `8ce9246f` ×3; the shape is modelled on the two briefs that ran to completion without clarification (`ce24fded`, `d463cf4c`).
